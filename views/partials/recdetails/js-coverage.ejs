<div>
    <h4>Rule: JavaScript code coverage</h4>
    <p><strong>Status: <%= getStatusName(model.status) %></strong>
    </p>
    <p>Serve only the JavaScript code needed by the page. Sending a whole lot of code which is not being used by the page will hurt performance as it will take network bandwidth and additional js parse time.</p>
    <h4>What this rule checks</h4>
    <ul>
        <li>If JavaScript code coverage is > 60: Passed </li>
        <li>If JavaScript code coverage is <= 60 && >25: Warning</li>
        <li>If JavaScript code coverage is <= 25: Failed</li>
    </ul>

    <% if(meta)
    {
    %>
    <div>
       <p>JavaScript code - Total bytes used: <%= Math.round(meta.jsTotalBytesUsed,2) %>  %</p>
    </div>
    <% } %>
</div>