<div>
    <h4>Rule: Script duration</h4>
    <p><strong>Status: <%= getStatusName(model.status) %></strong> </p>
    <p>If scripts are taking too much time to execute, it will affect the page performance.</p>
    <h4>What this rule checks</h4>
    <ul>
        <li>If the script parse-execution time <= 1: Passed </li>
        <li>If the script parse-execution time >1 and <=2: Warning</li>
        <li>If the script parse-execution time > 2: Failed </li>
    </ul>

    <% if(meta)
    {
    %>
    <div>
       <p>  Script duration : <%= meta.ScriptDuration %></p>
    </div>
    <% } %>
    <hr >
</div>