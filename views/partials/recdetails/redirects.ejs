<div>
    <h3>Redirect responses</h4>
    <p>Status :
        <%= getStatusName(model.status) %>
    </p>
    <p>A Redirect response instructs a browser to make a new request to the location header value given by server for the current request's response. An additional call will affect the performance.</p>
    <h4>What this rule checks</h4>
    <p>If number of redirect responses==0  : Passed </p>
    <p>If number of requests made >0 and <=2 : Warning </p>
    <p>If number of requests made >2: Failed </p>


    <%
   
    var allRequests = meta;
    if(allRequests)
    {
        var redirectRequests = allRequests.filter(function (item) {
            return (item.status == 302 || item.status == 307);
          });
       
        %>
        <table class="table table-striped small">
            <thead class="thead">
                <tr>
                    <th>URL</th>
                    <th>Type</th>
                    <th>Method</th>
                </tr>
            </thead>
            <% redirectRequests.forEach(function(item) { %>
                <tr>
                    <td>
                        <%= item.url %>
                    </td>
                    <td>
                            <%= item.type %>
                    </td>
                    <td>
                            <%= item.method %>
                    </td>
                </tr>
                <% }); %>
        </table>
        <%
    }
%>


</div>