<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>🚀:: Results</title>

    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u"
        crossorigin="anonymous">
    <style>
        .validation-item {
            overflow: auto;
            border-bottom: 1px solid gray;
            vertical-align: middle;

        }

        .res-img {
            width: 50px;
            height: 50px;
            float: left;
            margin: 10px;
            background-color:gray;
        }

        .res-img img {
            width: 100%;
            height: 100%;
        }

        .res-msg {
            padding: 5px;
            float: left;
            height: 50px;
            line-height: 50px;
            text-align: center;
        }

        .alert {
            padding: .75rem 1.25rem;
            margin-bottom: 1rem;
            border: 1px solid transparent;
            border-radius: .25rem;
        }

        .alert-danger {
            background-color: #f2dede;
            border-color: #ebcccc;
            color: #a94442;
            margin-top: 1rem;
        }
        .details
        {
            float:right;
            width:24px;
            height:24px;
        }
    </style>
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
</head>

<body>
    <div class="container">

        <div>
            <h4>
                <a target="_blank" href='<%= model.url%>'>
                    <%= model.url%>
                </a>
            </h4>
 

                        <hr>


                        <% include partials/ruleresults %>


                        <hr/>
                                        <div class="row">
                                            <p class="text-muted small text-center">Waited <%=  model.delay %> seconds before analysis.</p>

                                        </div>
        </div>

    </div>

    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.3.1/jquery.min.js"></script>
    
    <script>
        $(function () {
            $("a.show_details").click(function(e){
                e.preventDefault();
                var _this=$(this);
                var url=_this.attr("href");
                var _d=_this.closest(".recommendation-container").find(".details-meta");
                var arrow = _this.find(".material-icons");
                var activeClassName = arrow.text();
                var nextClassName = activeClassName=="arrow_forward"?"arrow_upward":"arrow_forward";

                if(_d.find("p").length==0)
                {
                    $.get(url).then(function(result){
                        _d.html(result);
                       
                    });
                }
                else
                {
                    if( activeClassName=="arrow_upward")
                    {
                        _d.hide();
                    }
                    else
                    {
                        _d.show();
                    }
                }
                arrow.text(nextClassName);
            });
        });
    </script>
</body>

</html>